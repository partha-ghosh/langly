import os
os.system('pip install flask flask_socketio deep_translator gTTS matplotlib numpy --no-cache-dir')

# pkg update
# termux-setup-storage
# pkg install proot-distro
# proot-distro install ubuntu
# proot-distro login ubuntu --user root --shared-tmp

# Proot storage location
# /data/data/com.termux/files/usr/var/lib/proot-distro/installed-rootfs/ubuntu
